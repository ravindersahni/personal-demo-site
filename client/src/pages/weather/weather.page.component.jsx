import React from 'react';
import WithErrorBoundary from '../../components/with-error-boundary/with-error-boundary.component';

export const WeatherPage = () => <h1>Weather</h1>;

export default WithErrorBoundary(WeatherPage);
